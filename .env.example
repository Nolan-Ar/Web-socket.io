# Configuration du serveur
# Copiez ce fichier en .env et modifiez les valeurs selon vos besoins

# Port du serveur (par défaut: 3000)
PORT=3000

# Mode de développement (development ou production)
NODE_ENV=development

# Configuration Socket.io (optionnel)
# CORS_ORIGIN=* (par défaut, accepte toutes les origines)
# En production, remplacez par votre domaine spécifique
# CORS_ORIGIN=https://votre-domaine.com
